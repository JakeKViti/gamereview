<h1 align="center"> <%= info %> </h1>
<p align="center">This is <%=@user.name%>'s page</p> 
<p align="center">If you disagree with them, let them know here <%=@user.email %> <p> </br>

<p align="center"><% @user.reviews.each do |review| %>
    <%= "#{review.game.title}:" %> 
    <%= link_to review.title, user_review_path(@user, review)%>
    <%=" (#{review.rating}/100)" %></br>
<%end%></p>

